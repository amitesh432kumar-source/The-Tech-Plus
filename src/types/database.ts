/**
 * Placeholder for Supabase-generated database types.
 * Regenerate with the Supabase MCP `generate_typescript_types` tool (or
 * `supabase gen types typescript`) once the schema exists, and replace this
 * file's contents with the generated `Database` type.
 */
export type Database = Record<string, unknown>;
